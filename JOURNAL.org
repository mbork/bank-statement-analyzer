#+TITLE: Bank Statement Analyzer
#+AUTHOR: mbork

Desktop app for loading bank CSV exports, deduplicating transactions,
assigning them to categories, and generating tabular spending reports
by category and time interval.  Built with Python (learning project),
SQLite, and Tkinter + ttkbootstrap.

* TODO CSV export (transactions table and report)
Export the full transactions table and current report to CSV format
for analysis in spreadsheets or other tools.

** Steps
- [ ] Design CSV format for transactions (columns, date format)
- [ ] Implement =transactions_to_csv()= in =reports.py=
- [ ] Implement =report_to_csv()= in =reports.py=
- [ ] Add export buttons to UI (transactions view and report view)
- [ ] Test CSV import/export round-trip in spreadsheet

* DONE Scaffold
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-02 Thu 14:46]
:END:
Create the project file structure, stub modules, and dev tooling.

** Steps [5/5]
- [X] Create directory structure (=bank_analyzer/=, =bank_analyzer/ui/=, =tests/=, =data/=)
- [X] Create stub Python files with Outli section headers and imports
- [X] Write =pyproject.toml= and run =uv sync= to generate =uv.lock=
- [X] Write =.env.example=
- [X] Write =.gitignore=

** Decisions
*** [2026-04-02] – Chose SQLite + Tkinter (Approach 1) over PySide6 or local web app
**** Reason
Fastest path to a working prototype; all dependencies are stdlib or
near-stdlib.  Explicit migration path to PySide6 planned: keep all
non-UI code in separate modules so =ui/= can be replaced independently.

**** Alternatives considered
- PySide6: better UI, but heavier dependency and steeper Qt learning curve
- Local web app (FastAPI + browser): rich UI, but not a true desktop app and
  harder to package

*** [2026-04-02] – Dedup via composite unique constraint, not hashing
**** Reason
=UNIQUE(date, amount, description)= + =ON CONFLICT DO NOTHING= is DB-enforced,
transparent to query, and easier to debug than a hash column.

**** Alternatives considered
Hash of canonical fields: same vulnerability to description variance, but
opaque column values make debugging harder.

*** [2026-04-02] – Description canonicalization required before insert
**** Reason
The composite unique constraint only works if the same transaction always
produces the same description string.  Raw CSV strings are unreliable
(trailing spaces, encoding differences, bank formatting changes).

*** [2026-04-02] – Use =uv= instead of =pip= for dependency management
**** Reason
=uv= provides a proper lock file (=uv.lock= ≈ =package-lock.json=), manages the
venv automatically, and replaces =pip=, =venv=, and =pip-tools= in one tool.
=requirements.txt= has no lock file concept; reproducible installs require pinning
by hand or a separate tool.

**** Alternatives considered
Plain =pip= + =requirements.txt=: simpler but no lock file, non-reproducible builds.

*** [2026-04-02] – Data location via =BANK_ANALYZER_DATA_DIR= env var
**** Reason
Allows dev (=./data=) and prod (OS user-data dir) to differ by one env var,
not by code changes.  =python-dotenv= loads =.env= automatically at startup.

** Session log
*** [2026-04-02] (session 2)
- Completed: full scaffold — =pyproject.toml=, =uv.lock=, =main.py=, all stub modules
  (type-hinted, =...= bodies), =.env.example=, =.gitignore=, =README.org= stub;
  hello-world ttkbootstrap window working; switched to spaces indentation
- In progress: NONE — scaffold DONE
- Next: start =config.py= (DB path resolution)
- Blockers: NONE
- Decisions: use =uv= instead of =pip=; spaces over tabs (Python convention)
*** [2026-04-02] (session 1)
- Completed: architecture decisions, =CLAUDE.md=, =JOURNAL.org=
- In progress: scaffold (not yet started)
- Next: create directory structure and stub files
- Blockers: NONE
- Decisions: all logged above

* DONE Set up Emacs for Python development
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-02 Thu 15:51]
- State "INPROGRESS" from "TODO"       [2026-04-02 Thu 14:53]
:END:
Configure Emacs for a comfortable Python workflow: LSP, venv awareness,
and test integration.

** Steps [5/5]
- [X] Configure Python major mode (=python-ts-mode= with tree-sitter, or built-in =python-mode=)
- [X] Install pyright and configure =eglot= to use it in Python buffers
- [X] Enable pyright's linting diagnostics in =eglot= / =flymake=
- [X] Set up venv detection so Emacs uses the project's =.venv= automatically
- [X] Configure test runner integration (=python-pytest.el=, =C-c t= → =python-pytest-dispatch=)

** Decisions
*** [2026-04-02] – Use pyright (not pylsp) as the LSP server; rely on it for linting
**** Reason
pyright covers both LSP and linting, so no separate linter tool is needed.

*** [2026-04-02] – Exclude =.venv= in =[tool.pyright]= in =pyproject.toml=
**** Reason
Without it, pyright tried to index =.venv= on connect and crashed (exit code 1).

*** [2026-04-02] – Use =pyvenv= with a =locate-dominating-file= hook for auto-activation
**** Reason
Automatically activates the project =.venv= on =python-ts-mode= without manual
=M-x pyvenv-activate=; works across multiple projects.

*** [2026-04-02] – Use =:after python= in =python-pytest= =use-package= block
**** Reason
Without it, =python-ts-mode-map= doesn't exist yet when the keybinding is set.

** Session log
*** [2026-04-02] (session 3)
- Completed: all 5 steps — =python-ts-mode=, eglot+pyright, flymake, pyvenv auto-activation,
  =python-pytest-dispatch= on =C-c t=
- Blockers resolved: pyright crashed on connect (fixed by =exclude = [".venv"]= in =pyproject.toml=);
  =C-c t= unbound (fixed by =:after python=); treesit display error is a benign Emacs 31 pre-release bug
- In progress: NONE — all steps done
- Next: =config.py= — DB path resolution

* DONE =config.py= — DB path resolution
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-07 Tue 08:40]
- State "INPROGRESS" from "TODO"       [2026-04-07 Tue 08:40]
- State "TODO"       from "BLOCKED"    [2026-04-02 Thu 14:52]
:END:
Resolve the database file path from env var or OS user-data directory.
Blocked by: [[*Scaffold]]

** Steps [4/4]
- [X] Implement =get_data_dir()=: read =BANK_ANALYZER_DATA_DIR= env var, fall back to OS user-data dir
- [X] Implement OS fallback: =~/.local/share/...= on Linux, =%APPDATA%\...= on Windows
- [X] Implement =get_db_path()=: combine data dir + filename
- [X] Ensure data directory is created if it does not exist

** Decisions
** Session log
*** [2026-04-07]
- Completed: =config.py= fully implemented — =get_data_dir()= (env var + OS fallback + mkdir), =get_db_path()=
- In progress: NONE
- Next: N/A — feature DONE
- Blockers: NONE
- Decisions: NONE

* DONE =db.py= — schema and queries
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-07 Tue 09:21]
- State "INPROGRESS" from "TODO"       [2026-04-07 Tue 08:40]
- State "TODO"       from "BLOCKED"    [2026-04-07 Tue 08:40]
:END:
Create the database schema and provide all SQL query functions.
Blocked by: [[*=config.py= — DB path resolution]]

** Steps [3/3]
- [X] Write =get_connection()= using =config.get_db_path()=
- [X] Write =create_schema()= with =CREATE TABLE IF NOT EXISTS= for =transactions=, =categories=, =imported_files=
- [X] Add =UNIQUE(date, amount, description)= constraint on =transactions=

** Decisions
** Session log
*** [2026-04-07]
- Completed: =get_connection()= (with =foreign_keys= PRAGMA + =row_factory=), =create_schema()= (3 tables, FK constraints, UNIQUE dedup constraint)
- In progress: NONE
- Next: N/A — feature DONE
- Blockers: NONE
- Decisions: store amounts in integer cents (grosz); =source_file= column references =imported_files= by FK not filename string

* DONE =importer.py= — CSV parsing and deduplication
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-13 Mon 11:16]
- State "INPROGRESS" from "TODO"       [2026-04-07 Tue 10:09]
- State "TODO"       from "BLOCKED"    [2026-04-07 Tue 09:29]
:END:
Parse bank CSV files, canonicalize descriptions, and insert with dedup.
Blocked by: [[*=db.py= — schema and queries]]

** Steps [7/7]
- [X] Define =BANKS= dict with per-bank config (date/amount/thousand separator,
  encoding, description_cols)
- [X] Write =get_anchor_cols(config)=: derive anchor column set from =date_col=,
  =amount_col=, and the base (non-synthetic) names in =description_cols=
- [X] Write =find_header_row(reader, anchor_cols)=: scan rows until all anchor cols are
  present; assign synthetic =LastNamed+N= names to unnamed columns; ignore extra named
  cols; fail with clear error if anchor cols are missing
- [X] Write =canonicalize_description()=: strip and collapse whitespace
- [X] Write =parse_amount(raw, config)=: apply regex, strip separators, return integer cents
- [X] Write =parse_csv(filepath, bank)=: return =list[dict]=
- [X] Write =import_file(filepath, bank)=: insert rows with =ON CONFLICT DO NOTHING=

** Decisions
*** [2026-04-07] – Encoding specified per bank in =BANKS= dict, not auto-detected
**** Reason
The bank is known in advance (Polish bank, CP-1250), so runtime detection adds
complexity with no benefit.  Each =BANKS= entry includes an =encoding= key passed
to =open()= / =csv.DictReader=.

**** Alternatives considered
=charset-normalizer= for runtime detection: accurate but an extra dependency,
unnecessary when the encoding is known.

*** [2026-04-09] – Header autodetection via anchor column set; synthetic names for unnamed columns
**** Reason
Banks may prepend unstructured preamble rows (variable count) before the CSV table,
and may rename or reorder named columns across export versions.  Scanning for a row
that contains all anchor column names is resilient to both.  Unnamed columns (empty
header cells) get synthetic names based on the last preceding named column
(e.g. =Opis=, =Opis+1=, =Opis+2=) so =col_map= can reference them stably.

**** Alternatives considered
- Hardcoded =skip_rows= count: breaks silently if preamble length changes.
- Config-as-module (functions per bank): maximum flexibility but end users cannot
  define new banks without writing Python — overkill for this use case.

**** Anchor columns
Derived at runtime from =date_col=, =amount_col=, and the base (non-synthetic) names
in =description_cols=.  Extra named columns not in the anchor set are silently ignored.

*** [2026-04-09] – Decimal and thousand separators explicit in bank config
**** Reason
Banks are inconsistent: some use a period as decimal separator, some a comma, and
thousand-grouping characters vary too.  Tolerating both separators without config
is ambiguous (=1.234= could be 1234 or 1.234).  Explicit per-bank =decimal_sep= and
=thousand_sep= keys remove the ambiguity and keep parsing deterministic.

**** Alternatives considered
Auto-detect based on position of separators: complex, error-prone for edge cases
like round numbers (=1.000=).

*** [2026-04-09] – =amount_regex= in bank config for banks that mix amount and currency in one column
**** Reason
Some banks (e.g. mBank) put amount and currency in the same column (e.g. =-43,52 PLN=).
An optional =amount_regex= key in the bank config holds a pattern with a named capture
group =amount= (e.g. =r'(?P<amount>-?[\d\s,.]+)\s+[A-Z]{3}'=) to extract just the
numeric part.  When absent (e.g. PKO BP), the raw column value is used directly.

**** Alternatives considered
A separate =amount_col= and =currency_col= split: requires the bank to always have
separate columns, which is not universally true.

*** [2026-04-09] – Multiple CSV columns joined into one =description= field
**** Reason
Some banks split transaction details across several columns (e.g. =Opis transakcji=
holds the title/reference while the next unnamed column holds the merchant location).
A =description_cols= list in the bank config names the columns to join; they are
concatenated with a space separator and then passed through =canonicalize_description()=,
so whitespace collapsing handles any padding between joined fragments automatically.

** Session log
*** [2026-04-13] (session 10)
- Completed: =test_duplicate_file_raises_error= and =test_duplicate_transactions_are_deduplicated=
  in =test_importer.py=; fixed =manage_connection()= in =db.py= (commit in normal path, not =finally=)
- In progress: NONE — all 7 steps done
- Next: mark feature DONE, commit, then start =categories.py=
- Blockers: NONE
- Decisions: =conn.commit()= after =yield= not in =finally= (rollback path must not commit);
  stats-only assertions sufficient for dedup test (no DB query needed)
*** [2026-04-10] (session 9)
- Completed: =import_file()= in =importer.py= (stats dict, =FileAlreadyImportedError=,
  =skipped= key); =manage_connection()= context manager in =db.py= (replaces =get_connection=);
  happy-path test scaffold in =test_importer.py= (fixture + test written, not yet committed)
- In progress: =import_file= tests (happy path written; duplicate-file and dedup tests pending)
- Next: add duplicate-file and dedup tests for =import_file=, run full suite, then commit
- Blockers: NONE
- Decisions: =manage_connection()= handles commit/rollback/close via =@contextmanager=;
  bare =except:= replaced with =except Exception:=; =FileAlreadyImportedError= (not =...Exception=) per Python convention
*** [2026-04-09] (session 8)
- Completed: `insert_imported_file` and `insert_transactions` in `db.py`;
  `tests/test_db.py` with fixture and 4 tests (basic insert, dedup, overlapping sets);
  decided to drop `get_import_stats` — `import_file` will return stats directly
- In progress: NONE
- Next: implement `import_file(filepath, bank)` in `importer.py` — call `parse_csv`,
  open connection, call both db helpers inside a transaction, return stats dict
- Blockers: NONE
- Decisions: `get_import_stats` dropped; stats returned directly from `import_file`

*** [2026-04-09] (session 7)
- Completed: =parse_amount(raw, bank)= (regex, separator handling, integer cents);
  =parse_csv(filepath, bank)= (header detection, DictReader, empty row skipping, =list[dict]=);
  parametrized invalid-input tests for =parse_amount=; fixture-based smoke tests for =parse_csv=
  (mBank + 2 overlapping PKO BP fixtures)
- In progress: NONE
- Next: implement =import_file(filepath, bank)= — insert rows with =ON CONFLICT DO NOTHING=
- Blockers: NONE
- Decisions: =parse_amount= kept public (private naming caused Pyright false positives);
  =DictReader= requires explicit =delimiter= matching bank config

*** [2026-04-09] (session 6)
- Completed: =BANKS= dict (PKO BP + mBank), =canonicalize_description()=,
  =get_anchor_cols()=, =find_header_row()=; tests for all three; mBank fixture added
- In progress: NONE
- Next: implement =parse_csv(filepath, bank)=
- Blockers: NONE
- Decisions: anchor cols derived from config (not hardcoded); extra named columns
  silently ignored; =column_sep= added to bank config; =amount_regex= for mBank

*** [2026-04-09]
- Completed: architecture decisions for header autodetection, unnamed column naming,
  separator config; steps updated; future =required_cols= work logged as deferred task
- In progress: NONE — no code written yet
- Next: implement =BANKS= dict and =find_header_row()=, then =canonicalize_description()=
- Blockers: NONE
- Decisions: all logged above

*** [2026-04-07]
- Completed: encoding decision logged; anonymized CSV fixture added to =tests/fixtures/=
- In progress: NONE — no code written yet
- Next: implement =canonicalize_description()=, then define =BANKS= dict with column names, date format, encoding
- Blockers: NONE
- Decisions: encoding per bank in =BANKS= (already logged above)

* DONE Add =ruff= and configure it
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-13 Mon 12:24]
- State "INPROGRESS" from "TODO"       [2026-04-13 Mon 12:24]
:END:
Add =ruff= as a dev dependency and configure line length and any other rules in =pyproject.toml=.

** Steps [2/2]
- [X] Add =ruff= with =uv add --dev ruff=
- [X] Configure =[tool.ruff]= in =pyproject.toml= (line length, selected rules)

** Decisions
*** [2026-04-13] – Rule set: E, F, W, I, N, UP, B, C4, SIM, PT
**** Reason
Covers real bugs (F, B), style (E, W), imports (I), modern syntax (UP),
and pytest idioms (PT) without being pedantic.  N included despite deliberate
naming conventions — can suppress per-file if needed.

*** [2026-04-13] – Suppress E501 per-file for =tests/test_importer.py=
**** Reason
Long lines are real bank transaction description strings that cannot be
shortened without losing fidelity.

** Session log
*** [2026-04-13]
- Completed: =ruff= added as dev dependency; configured in =pyproject.toml=
  (line-length 100, 10 rule groups, per-file E501 ignore for test_importer.py);
  all 24 lint errors fixed across =db.py=, =importer.py=, =test_db.py=,
  =test_importer.py=, =test_reports.py=
- In progress: NONE — mark as DONE
- Next: N/A
- Blockers: NONE
- Decisions: rule set and per-file ignore logged above

* DONE =categories.py= — category CRUD
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-13 Mon 14:01]
- State "INPROGRESS" from "TODO"       [2026-04-13 Mon 12:26]
- State "TODO"       from "BLOCKED"    [2026-04-07 Tue 09:29]
:END:
Manage the list of spending categories.
Blocked by: [[*=db.py= — schema and queries]]

** Steps [4/4]
- [X] Write =get_all_categories()=
- [X] Write =add_category(name)=
- [X] Write =rename_category(category_id, new_name)=
- [X] Write =delete_category(category_id)=

** Decisions
*** [2026-04-13] – SQL helpers in =db.py= accept =conn=; =categories.py= manages connection internally
**** Reason
All SQL must live in =db.py= (per architecture rule), so the testable layer is already
=db.py=.  =categories.py= functions call =db.py= helpers and own the =manage_connection()=
call, matching the pattern established by =importer.py=.  Tests exercise =db.py= helpers
directly with a test =conn=, as in =test_db.py=.

**** Alternatives considered
=categories.py= functions accept =conn= (caller manages connection): would make
=categories.py= trivially thin pass-throughs to =db.py=, duplicating the interface
without adding value.

*** [2026-04-13] – =UNIQUE= constraint on =categories.name= enforced in schema
**** Reason
DB-enforced uniqueness is reliable and race-free; raises =IntegrityError= on conflict,
which =categories.py= can catch and re-raise as a domain exception.  A Python-level
prior =SELECT= check would have a TOCTOU race and is less reliable.

** Session log
*** [2026-04-13] (session 11)
- Completed: four SQL helpers in =db.py= (=get_all_categories=, =insert_category= with =RETURNING=,
  =update_category=, =delete_category=); =UNIQUE= constraint on =categories.name=;
  10 tests in =test_db.py=; all four wrappers in =categories.py= using =manage_connection()=
- In progress: NONE — feature DONE
- Next: start =ui/app.py= or =reports.py= (both unblocked)
- Blockers: NONE
- Decisions: =insert_category= uses =RETURNING= (no follow-up SELECT needed);
  =update_category= and =delete_category= raise =ValueError= if no row matched

* DONE =ui/app.py= — root window and navigation
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-13 Mon 14:40]
- State "INPROGRESS" from "TODO"       [2026-04-13 Mon 14:40]
- State "TODO"       from "BLOCKED"    [2026-04-07 Tue 09:29]
:END:
Main application window; wires up DB init and hosts all views.
Blocked by: [[*=db.py= — schema and queries]]

** Steps [3/3]
- [X] Subclass ttkbootstrap =Window=, initialize DB on startup
- [X] Add navigation (notebook tabs)
- [X] Implement =run()= entry point called from =main.py=

** Decisions
*** [2026-04-13] – Call =create_schema()= in =run()=, not in =App.__init__=
**** Reason
Keeps =App.__init__= focused on UI construction.  Infrastructure (DB schema) is
set up before the window is created, matching the pattern of =load_dotenv()= in
=main.py=.  Errors from schema creation can be handled before any window exists.

**** Alternatives considered
Call =create_schema()= inside =App.__init__=: self-contained but mixes UI and DB
concerns; harder to handle errors before the window is visible.

** Session log
*** [2026-04-13]
- Completed: =App= subclasses =ttk.Window=; =Notebook= with four tabs (Import,
  Transactions, Reports, Categories); =create_schema()= called in =run()=;
  =categories_view.py= stub created; tab ordering decided; decision logged
- In progress: NONE — feature DONE
- Next: start =reports.py= (spending queries, unblocked)
- Blockers: NONE
- Decisions: tab order Import → Transactions → Reports → Categories (Rules deferred
  pending =categorizer.py=); =create_schema()= in =run()= (already logged above)

* DONE =ui/import_view.py= — CSV import screen
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-13 Mon 16:40]
- State "INPROGRESS" from "TODO"       [2026-04-13 Mon 16:40]
- State "TODO"       from "BLOCKED"    [2026-04-13 Mon 14:41]
:END:
Let the user pick a CSV file and bank, run the import, and see results.
Blocked by: [[*=importer.py= — CSV parsing and deduplication]], [[*=ui/app.py= — root window and navigation]]

** Steps [0/4]
- [ ] Bank selector dropdown (populated from =BANKS= dict)
- [ ] File picker button (opens native file dialog)
- [ ] Import button triggering =importer.import_file()=
- [ ] Status label showing rows imported/skipped

** Decisions
- Switching entire =ui/= from ttkbootstrap/Tkinter to PySide6.  ttkbootstrap
  1.20.2 uses PIL's =_imagingtk= to render widget images; on Arch Linux with
  Python 3.13 this causes a fatal XCB threading crash (=XInitThreads()= called
  after X11 is already in use).  Multiple workarounds attempted (OptionMenu
  instead of Combobox, Pillow downgrade to 10.4.0, pre-importing PIL.ImageTk,
  calling =XInitThreads()= via ctypes before Tk init) all failed.  PySide6
  avoids the problem entirely — Qt owns its X11/threading lifecycle.  The
  architecture was already designed for this migration (=ui/= isolation).

** Session log
*** [2026-04-13] (earlier)
- Implemented =ui/import_view.py= (all 4 steps: bank dropdown, file picker,
  import button, status label) using ttkbootstrap
- Hit fatal XCB crash (PIL + ttkbootstrap + Python 3.13 incompatibility);
  multiple fixes attempted, none succeeded
- Decision: migrate =ui/= to PySide6 (see Decisions above)
- Blockers: NONE

*** [2026-04-13] (later)
- Removed =ttkbootstrap= + =pillow=; added =pyside6=
- Migrated all =ui/= stubs to PySide6 (=QWidget=, =QMainWindow=, =QTabWidget=)
- Reimplemented =ui/import_view.py= in PySide6 — emits =import_succeeded=
  signal after a successful import
- Implemented =ui/transactions_view.py= — read-only =QTableWidget=, reverse
  chronological, auto-refreshes via =import_succeeded= signal; description
  column stretches, category fixed at 2× amount width
- Added =get_all_transactions()= to =db.py= (LEFT JOIN categories, newest-first)
- In progress: NONE — mark =import_view= DONE; =transactions_view= INPROGRESS
- Next: =ui/categories_view.py=, =ui/reports_view.py=
- Blockers: NONE

* DONE =ui/categories_view.py= — manage categories
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-14 Tue 08:28]
- State "INPROGRESS" from "TODO"       [2026-04-14 Tue 08:28]
- State "TODO"       from "BLOCKED"    [2026-04-13 Mon 14:41]
:END:
Allow the user to add, rename, and delete categories.
Blocked by: [[*=categories.py= — category CRUD]], [[*=ui/app.py= — root window and navigation]]

** Steps [3/3]
- [X] List all categories in a =QListWidget=
- [X] Add category (entry field + button calling =categories.add_category()=)
- [X] Rename and delete selected category

** Decisions
** Session log

* DONE =ui/transactions_view.py= — browse and categorize
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-14 Tue 11:02]
- State "INPROGRESS" from "TODO"       [2026-04-13 Mon 16:40]
- State "TODO"       from "BLOCKED"    [2026-04-13 Mon 14:41]
:END:
Show all transactions in a table; allow assigning categories.
Blocked by: [[*=categories.py= — category CRUD]], [[*=ui/app.py= — root window and navigation]]

** Steps [5/5]
- [X] Table showing date, description, amount, category (read-only for now)
- [X] Category dropdown for assigning a category to selected rows
- [X] Filters (date range, category, description) — UI + =get_all_transactions= tests:
  - [X] =test_get_all_transactions_filters_by_category=
  - [X] =test_get_all_transactions_no_filter_returns_all=
  - [X] =test_get_all_transactions_filters_by_date_from=
  - [X] =test_get_all_transactions_filters_by_date_to=
  - [X] =test_get_all_transactions_filters_by_date_range=
  - [X] =test_get_all_transactions_filters_by_description=
  - [X] =test_get_all_transactions_filters_by_description_case_insensitive_polish=
  - [X] =test_get_all_transactions_combined_filters=
- [X] Amount range filter (=abs()= logic) — =db.py= + UI + tests:
  - [X] =test_get_all_transactions_filters_by_amount_min=
  - [X] =test_get_all_transactions_filters_by_amount_max=
  - [X] =test_get_all_transactions_filters_by_amount_range=
  - [X] =test_get_all_transactions_amount_filter_includes_income=
- [X] Sort by column (header click); handle amount column numerically

** Decisions
- Read-only for now; category assignment deferred to later step
- =import_succeeded= signal (on =ImportView=) drives auto-refresh — avoids
  polling and keeps tabs decoupled

** Session log
*** [2026-04-14] (3)
- Completed: designed spending report API (=spending_report()= with granularity
  param, century-as-Roman-numeral trick, window function for %, live updates
  with debounce); consolidated =reports.py= + =ui/reports_view.py= into one task;
  added spending chart as BLOCKED task; updated all journal steps with [me]/[Claude] tags
- In progress: NONE
- Next: start =Make tests more DRY= or =spending_report()= in =reports.py=
- Blockers: NONE
- Decisions: live report updates (no Run button) with 200ms debounce on category
  selector; grouping/Roman numeral helpers written by user, unit-testable

*** [2026-04-14] (2)
- Completed: fixed calendar popup default (`_DefaultPageCalendar` subclass with
  `showEvent` + `setSelectedDate` + `blockSignals`); amount range filter
  (`amount_min`/`amount_max` in `TransactionFilters`, `abs()` SQL, UI with
  `QDoubleValidator`); Unicode `lower` override on SQLite connection; full
  filter test suite (35 tests, incl. 6 parametrized Polish case combinations)
- In progress: NONE
- Next: write tests for `Make tests more DRY` step, or start next feature
- Blockers: NONE
- Decisions: `showPopup` not overridable in PySide6 for `QDateTimeEdit` —
  solved via `setCalendarWidget(_DefaultPageCalendar(...))`

*** [2026-04-14]
- Completed: category assignment panel (combobox + Assign, multi-row, pre-selects
  current); =categories_changed= signal propagates renames/deletes to transactions
  view; column sorting with =_SortableItem= for correct numeric amount sort;
  =TransactionFilters= dataclass + filter bar (date range, category, description)
  with =_DateFilterEdit= calendar popup; friendly FK error on category delete
- In progress: filter tests not yet written
- Next: write =test_db.py= tests for all =TransactionFilters= combinations
- Blockers: NONE
- Decisions: =TransactionFilters= dataclass over multiple params (extensible,
  single signature); =categories_changed= signal pattern mirrors =import_succeeded=

*** [2026-04-13]
- Implemented read-only =QTableWidget= (date, description, amount in PLN,
  category); reverse chronological via =get_all_transactions()= in =db.py=
- Dynamic column widths: description stretches, others size to content,
  category fixed at 2× amount width
- Auto-refresh wired via =import_succeeded= signal in =App.__init__=
- In progress: category assignment and filters deferred
- Next: =ui/categories_view.py=
- Blockers: NONE

* TODO Internationalization (English/Polish)
Support English and Polish UI, selected by system locale or manually by the
user.  Language change takes effect after restart.

** Steps [1/5]
- [X] Wrap all user-visible strings in =ui/= with =self.tr()=
- [ ] Run =lupdate= to extract strings into a =.ts= file; create Polish translation
- [ ] Run =lrelease= to compile =.ts= → =.qm= binary
- [ ] Auto-detect language at startup via =QLocale.system()= and load the right =.qm=
- [ ] Add manual language selection (combo box in settings or menu); store preference; restart to apply

** Decisions
** Session log

* DONE Manual transaction categorization
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-14 Tue 11:04]
- State "INPROGRESS" from "TODO"       [2026-04-14 Tue 11:04]
- State "TODO"       from "BLOCKED"    [2026-04-14 Tue 11:02]
:END:
Allow the user to assign or change a category on individual transactions
(or a selection) via the UI.
Blocked by: [[*=categories.py= — category CRUD]], [[*=ui/transactions_view.py= — browse and categorize]]

** Steps [3/3]
- [X] Add =set_transaction_category(conn, transaction_id, category_id)= to =db.py=
- [X] Wire category dropdown / combobox in =ui/transactions_view.py= to call it
- [X] Support bulk assignment (multiple selected rows → same category)

** Decisions
** Session log

* TODO =categorizer.py= — rule-based auto-categorization
:LOGBOOK:
- State "TODO"       from "BLOCKED"    [2026-04-13 Mon 14:41]
:END:
Apply saved substring rules to uncategorized transactions; wire into import
and expose as an on-demand call.
Blocked by: [[*=categories.py= — category CRUD]]

** Steps [0/5]
- [ ] Add =categorization_rules= table to schema: =rule_id=, =pattern= (text), =category_id= (FK)
- [ ] Write =db.py= helpers: =insert_rule=, =get_all_rules=, =delete_rule=
- [ ] Write =categorize_transactions(conn)=: apply all rules to uncategorized transactions only
- [ ] Wire =categorize_transactions= into =import_file()= (run after insert)
- [ ] Expose =categorize_transactions= for on-demand call from the UI

** Decisions
*** [2026-04-13] – Substring match (not regex) for rules
**** Reason
Simpler to enter in a UI; sufficient for typical merchant name matching.  Regex
adds power the user would rarely need and makes rule authoring harder.

*** [2026-04-13] – Rules applied both at import time and on-demand
**** Reason
Automatic application at import is the main convenience; on-demand re-run lets
the user apply newly added rules to already-imported transactions without
re-importing.

*** [2026-04-13] – Categorizer skips already-categorized transactions (MVP)
**** Reason
Preserves manual corrections — if the user overrides a rule by hand, a
subsequent auto-run must not undo that.  Future: add a "recategorize since
date" mode (see deferred task at end of file).

*** [2026-04-13] – Categorization rules are global (not bank-scoped)
**** Reason
Most merchants appear across all banks with similar descriptions.  Bank-scoped
rules would require duplicating every rule per bank — a real usability cost for
a problem most users will never hit.  No =bank= column on
=categorization_rules=, no escape hatch, to keep the MVP simple.

**** Alternatives considered
Optional nullable =bank= FK on =categorization_rules= (global when =NULL=):
ruled out to avoid schema and UI complexity in the MVP.

** Session log

* DONE Spending reports (=reports.py= + =ui/reports_view.py=)
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-15 Wed 09:49]
- State "INPROGRESS" from "TODO"       [2026-04-15 Wed 08:24]
- State "TODO"       from "BLOCKED"    [2026-04-13 Mon 14:03]
:END:
Spending summary by category and time interval: backend query + UI.
Blocked by: [[*=categories.py= — category CRUD]]

** Steps [4/4]
- [X] [me] Write =spending_report(date_from, date_to, granularity, category_ids)= in
  =reports.py=: returns flat list of =(period, category, total_grosz, percentage)=
  rows; =granularity= is one of =month= / =quarter= / =year= / =century=; =century=
  uses actual calendar centuries (I, II, …, XXI, …) displayed as Roman numerals,
  so the query structure is always uniform (current data falls under "XXI");
  percentage computed via =SUM(...) OVER (PARTITION BY period)= window function
- [X] [me] Write unit-testable helper functions (in =reports.py= or a separate
  module): Arabic→Roman numeral conversion; grouping of flat rows by period
  (returns e.g. =list[tuple[str, list[Row]]]=); these must have =pytest= tests
- [X] [Claude] Controls in =ui/reports_view.py=: date range pickers (from / to),
  four granularity radio buttons (month / quarter / year / century), multi-select
  category checkboxes
- [X] [Claude] Two-level table: period as section header rows, one category per
  data row with amount and percentage columns; uses grouping helper from step 2;
  report updates live on any control change (no "Run" button); category selector
  uses a 200ms =QTimer.singleShot= debounce to avoid re-running on every tick
  while the user is selecting multiple categories

** Decisions
*** [2026-04-14] – Periods derived from actual data, not from date range
**** Reason
=date_from= and =date_to= are optional (=None= = open-ended).  Even when set,
periods are derived from transactions present in the filtered result, not
generated from the date bounds.  A =date_from= of =2000-01-01= with the first
transaction in =2026-03= must not produce 26 years of empty rows.

*** [2026-04-14] – =category_ids= is =list[int | None]=; no outer =None= allowed; =[]= returns empty
**** Reason
Forbidding outer =None= (meaning "all categories") forces the caller to be
explicit; the UI always knows the full category list and passes it in.
=None= inside the list is a sentinel for "uncategorized" transactions
(=category_id IS NULL=), handled via a split into real IDs + =IS NULL= clause.
This avoids the SQL NULL trap (=NULL IN (...)= is always NULL, not TRUE).

**** Alternatives considered
- Outer =None= meaning "all": implicit, hides dependency on category list in caller.

*** [2026-04-14] – =spending_report= always excludes income; flips sign of expenses
**** Reason
Report operates on expenses only (=WHERE amount < 0=); amounts are returned as
positive values (=SELECT -amount=) for readability.  Income entries are
always excluded — the report is a spending report, not a cash-flow report.

*** [2026-04-14] – =date_from=/=date_to= use =datetime.date= in both =spending_report= and =TransactionFilters=
**** Reason
=datetime.date= is the natural Python type; passing strings risks silent
misformatting.  =TransactionFilters= converted at the same time for
consistency.  Callers in =ui/= use =QDate.toPython()= (one-liner).

**** Alternatives considered
- Keep =str= in =TransactionFilters= for backwards compat: not worth it — the
  UI layer was already doing =.toString('yyyy-MM-dd')= which is equally fragile.

*** [2026-04-14] – Single combined report function replacing two separate ones
**** Reason
One function covers all use cases: time granularity is a parameter, not a
separate query.  "Century" granularity replaces "all time" so the GROUP BY
structure is always =period, category= with no special-casing.  Percentage
column uses a SQLite window function to avoid Python post-processing.

**** Alternatives considered
- =spending_by_category= + =spending_by_month(category_id)=: two functions,
  single-category limitation, no uniform structure — replaced by this design.

** Session log
*** [2026-04-15]
- Completed: `spending_report()` implemented in `reports.py` (filtering,
  period expressions, window function for %, sign flip); `TransactionFilters`
  converted to `datetime.date`; shared `conn` fixture moved to `conftest.py`;
  comprehensive test suite in `test_reports.py` (17 tests, all passing)
- In progress: NONE
- Next: write helper functions (Arabic→Roman, group rows by period) + their
  tests; then step 3 (UI controls in `ui/reports_view.py`)
- Blockers: NONE
- Decisions: `category_ids: list[int | None]`, no outer `None`, `[]` = empty;
  income excluded, sign flipped; `datetime.date` throughout; periods from
  actual data, not generated from date bounds

* TODO Fill in =README.org=

** Steps [0/6]
- [ ] Requirements (minimum Python version, =uv= install link, Linux/Windows support)
- [ ] Installation (clone, =uv sync=, =cp .env.example .env=)
- [ ] Configuration (explain =BANK_ANALYZER_DATA_DIR= and the prod fallback)
- [ ] Usage (=uv run python main.py=; expand as features are added)
- [ ] Development (how to run tests; pointer to =BANKS= in =importer.py= for new banks)
- [ ] License (choose and state a license; MIT recommended)

** Decisions
** Session log

* DONE Apply ttkbootstrap theme
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-13 Mon 15:19]
- State "INPROGRESS" from "TODO"       [2026-04-13 Mon 15:19]
- State "TODO"       from "BLOCKED"    [2026-04-13 Mon 15:19]
:END:
Apply a consistent visual theme across all views.
Blocked by: [[*=ui/app.py= — root window and navigation]]

** Steps [2/2]
- [X] Choose theme (e.g. =flatly= or =litera=)
- [X] Verify consistent styling across all views

** Decisions
** Session log
*** [2026-04-13]
- Completed: =litera= theme applied via =themename= arg to =ttk.Window=
- In progress: NONE — feature DONE
- Next: N/A
- Blockers: NONE
- Decisions: NONE

* DONE Set up GitHub Actions
:LOGBOOK:
- State "DONE"       from "INPROGRESS" [2026-04-13 Mon 15:55]
- State "INPROGRESS" from "TODO"       [2026-04-13 Mon 15:55]
:END:
CI pipeline running tests on Linux and Windows; release pipeline building
standalone executables.

** Steps [4/4]
- [X] Write =.github/workflows/ci.yml= — run =pytest= on push (Linux + Windows matrix)
- [X] Add PyInstaller to dev dependencies (=uv add --dev pyinstaller=)
- [X] Write =.github/workflows/build.yml= — build executables on tag/release (Linux + Windows)
- [X] Verify executables run without Python installed

** Decisions
*** [2026-04-13] – CI on push to =master=; build on tags and =workflow_dispatch=
**** Reason
CI catches regressions on every push; build pipeline produces release
artifacts only when explicitly triggered (tag or manual).  =workflow_dispatch=
allows manual builds without tagging.

*** [2026-04-13] – Linux runner requires =python3-tk= installed before PyInstaller
**** Reason
Ubuntu GitHub Actions runners do not include tkinter by default — it is a
separate system package.  Windows runner includes it in the standard Python
install so no extra step needed.

** Session log
*** [2026-04-13]
- Completed: =ci.yml= (pytest on push/PR, Linux); =build.yml= (PyInstaller
  Linux + Windows, artifacts uploaded); both verified working; action versions
  updated to Node.js 24-compatible releases
- In progress: NONE — all 4 steps done; mark feature DONE
- Next: N/A
- Blockers: NONE
- Decisions: logged above

* TODO [Future] Recategorize transactions using updated rules
Allow the user to re-run categorization rules against already-categorized
transactions, optionally scoped to transactions since a specified date.
Deferred from MVP: =categorizer.py= currently skips transactions that already
have a category assigned.

** Steps [0/2]
- [ ] Add a =recategorize_since(conn, date)= function in =categorizer.py= that
  applies all rules to transactions on or after =date=, overwriting existing
  category assignments
- [ ] Wire into UI (e.g. "Re-apply rules since..." dialog)

** Decisions
** Session log

* TODO Configure eglot to run pyright + ruff simultaneously
Show ruff diagnostics inline in Emacs alongside pyright, using a
multi-server eglot setup.

** Steps [0/2]
- [ ] Identify the correct package/mechanism for multiple eglot servers
  per mode (name TBD — research needed)
- [ ] Configure eglot to attach both =pyright= and =ruff server= to
  =python-ts-mode= buffers

** Decisions
** Session log

* TODO Make tests more DRY
Reduce boilerplate in =tests/test_db.py= by reusing the =transaction_id= fixture.

** Steps [0/1]
- [ ] Refactor =test_delete_category_raises_when_referenced_by_transaction= to use
  the =transaction_id= fixture instead of manually calling =insert_imported_file=,
  =insert_transactions=, and a raw =SELECT transaction_id=

** Decisions
** Session log

* TODO Multi-select category filter

Allow selecting a set of categories (including "uncategorized") in the
transactions filter bar, instead of a single category.

** Steps [0/2]
- [ ] Change =TransactionFilters.category_id= to =category_ids: list[int | None] | None=;
  update SQL to =WHERE (category_id IN (?, ...) OR category_id IS NULL)= with the
  =IS NULL= branch added only when =None= is in the list
- [ ] Replace =_filter_category_combo= (=QComboBox=) with a multi-select widget;
  preserve selected set across refreshes; show summary label (e.g. "3 categories")

** Decisions
** Session log

* TODO Amount range filter in transactions view

** Decisions
*** [2026-04-14] – Amount filter uses =abs()= by default; raw mode activated by explicit sign prefix
**** Reason
=abs()= matches natural language ("show me transactions around 300 PLN") and works
for both income and expenses without requiring the user to think about signs.
Raw mode (=min <= amount <= max=) is activated when either input contains an
explicit =+= or =-= prefix, which requires deliberate intent.  A visual indicator
shows the current mode.  Erroneous/weird combinations (e.g. raw mode with
=min > max=, or a mixed-sign impossible range) are flagged visually rather than
silently returning an empty table.

**** Alternatives considered
- Always =abs()=: can't isolate expenses from income by amount — ruled out.
- Implicit sign switch (=abs= unless a value is negative): silent mode change,
  indistinguishable from a typo — ruled out.
- Explicit toggle (All / Expenses / Income): solves the problem cleanly but
  user didn't want the extra widget.

** Steps [0/1]
- [ ] Add min/max amount inputs to filter bar; implement sign-aware =abs()= logic
  in =db.py= / =TransactionFilters=; add visual mode indicator and highlight
  impossible/weird combinations

** Session log

* BLOCKED Spending chart in reports view
Stacked bar chart of spending by period, one bar per time bucket, segments
per category.
Blocked by: [[*Spending reports (=reports.py= + =ui/reports_view.py=)]]

** Steps [0/3]
- [ ] Add =matplotlib= dependency; embed =FigureCanvasQTAgg= in =ui/reports_view.py=
- [ ] Render stacked bar chart from =spending_report()= data: x-axis = periods,
  y-axis = amount, one color per category; group remainder as "Other" if >8
  categories selected
- [ ] Add tooltip or bar-segment label showing percentage

** Decisions
*** [2026-04-14] – matplotlib via FigureCanvasQTAgg; stacked bar; cap at 8 categories
**** Reason
Stacked bar shows both period trend and category breakdown simultaneously.
matplotlib is the natural choice for a personal tool (no extra Qt charting dep).
Cap at 8 categories to keep colors distinguishable; overflow bucketed as "Other".

** Session log

* Config                                                           :noexport:
#+SEQ_TODO: TODO(!) INPROGRESS(!) | DONE(!)
#+SEQ_TODO: | CANCELED(!)
#+SEQ_TODO: BLOCKED(!) TODO(!) |
#+PROPERTY: CLOCK_INTO_DRAWER t
#+PROPERTY: LOG_INTO_DRAWER t
#+CATEGORY: bank-statement-analyzer
