#+TITLE: Bank Statement Analyzer — Help

* Overview
# Things to write:
# - What the app does (import CSV bank statements, categorize transactions, generate spending reports)
# - Typical workflow: import -> categorize -> view reports
# - Note about demo mode (what it is, how to launch it, data is not saved)
# - Note that data is stored locally on the user's machine (no cloud, no account needed)

* Import
# Things to write:
# - How to select the bank from the dropdown (list supported banks)
# - How to browse for a CSV file exported from your bank
# - What happens when you click Import (success message, deduplication — same file can be imported safely twice)
# - Error messages: file not found, wrong format, unsupported bank
# - Where to get the CSV file from your bank (brief note; may vary by bank)

* Transactions
# Things to write:
# - Overview of the transactions table (columns: date, description, amount, category)
# - Income vs. expense (visual distinction — income shown in grey)
# - Filtering:
#   - Date range (From/To date pickers, calendar popup, "(any)" sentinel meaning no bound)
#   - Amount range (min/max fields)
#   - Category filter dropdown (including "(all)" and "(uncategorized)")
#   - Description text search (substring match)
#   - Clear filters button
# - Sorting columns by clicking headers
# - Selecting transactions (single and multi-select)
# - Assigning a category to selected transactions manually (Assign button)
# - Re-categorize button (applies all rules to all transactions, overwrites existing categories)
# - Exporting filtered transactions to CSV

* Reports
# Things to write:
# - Overview: spending grouped by category over a time period
# - Date range filter (From/To pickers) and quick presets: This month, Last month, All time
# - Granularity options: Month, Quarter, Year, Century (what each means in the table)
# - Category filter: selecting/deselecting categories with All/None shortcuts
# - Reading the report table (columns: period, category, amount, percentage)
# - Uncategorized transactions in reports
# - Exporting report to CSV

* Categories
# Things to write:
# - What categories are and how they're used
# - Adding a new category (type name, press Enter or click Add)
# - Renaming a category (select, click Rename)
# - Deleting a category (select, click Delete; warn about effect on categorized transactions)
# - Category names must be unique (error if duplicate)

* Rules
# Things to write:
# - What rules are: pattern -> category mappings for automatic categorization
# - Pattern matching (case-insensitive substring match against transaction description)
# - Adding a rule (enter pattern, select category, click Add rule or press Enter)
# - Deleting a rule (select row, click Delete rule)
# - How rules interact with Re-categorize (in Transactions tab)
# - Rules do not automatically apply on import — Re-categorize must be run manually
# - Rule order / conflict resolution (if multiple rules match)

* Settings
# Things to write:
# - Language selector: available languages (English, Polish, system default)
# - Applying changes requires restarting the app
# - What "system default" means (uses OS locale)
